# React Native Audiobook Player

This is a demo app to reproduce audiobooks audio player.

## Install:

1. `git clone https://github.com/andrewazevedo/best_player.git`
2. `cd best-player`
3. `yarn install`

## iOS:

`react-native run-ios`

## Android:

`react-native run-android`
